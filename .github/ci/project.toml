# Generated by velnor-workflow. Regenerate; do not hand-edit.
schema = 3
repository = "tailrocks/homebrew-velnor"
profile = "generic"
verified = true
default_branch = "main"
providers = ["github-hosted"]
automatic_providers = ["github-hosted"]
default_dispatch_providers = ["github-hosted"]

[analysis]
method = "static-filesystem-and-manifest-inspection"
detected = []
limitations = ["Project code, build scripts, task runners, and commands are never executed during analysis.", "Release, signing, registry, deployment, branch-protection, and runner-capability contracts remain explicit manual inputs."]

[workflow]
files = ["ci-main.yml", "ci-policy.yml", "ci-pr.yml", "ci-unit-homebrew.yml", "maintenance.yml", "nightly.yml"]

[release]
enabled = false
reason = "Release is fail-closed. Enable only after declaring immutable artifact, registry, provenance, and tag-protection policy."

[[unit]]
id = "homebrew"
label = "Homebrew"
kind = "homebrew"
root = "."
watch = ["Brewfile", "Casks/**", "Formula/**"]
pr_commands = ["mise run brew:tap-local", "brew audit --strict --online", "mise run formula:contract"]
full_commands = ["mise run brew:tap-local", "brew audit --strict --online", "mise run formula:contract"]
depends_on = ["tap-local"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = false
nested_privileged_docker = false
buildx_compose = false
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false

[[unit]]
id = "tap-local"
label = "Tap local checkout"
kind = "homebrew"
root = ""
watch = []
pr_commands = ["mise run brew:tap-local"]
full_commands = ["mise run brew:tap-local"]
platform = "linux-x64"
trust = "untrusted-ok"
[unit.capabilities]
docker = false
nested_privileged_docker = false
buildx_compose = false
testcontainers = false
services_with_readiness = false
browser_binaries = false
native_macos_arm64 = false
